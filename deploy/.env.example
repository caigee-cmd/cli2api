# Optional first-run bootstrap value. Leave blank to generate one and store it in SQLite.
PROXY_API_KEY=
# CLI2API_IMAGE=ghcr.io/caigee-cmd/cli2api:latest
# QODER_WORKER_BASE_PORT=32100
# QODER_MAX_INFLIGHT=4
